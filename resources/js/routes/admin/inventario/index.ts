import ajustes from './ajustes'
import kardex from './kardex'

const inventario = {
    ajustes: Object.assign(ajustes, ajustes),
    kardex: Object.assign(kardex, kardex),
}

export default inventario